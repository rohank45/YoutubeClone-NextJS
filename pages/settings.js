import React from "react";
import PageLayout from "../lib/components/PageLayout";

const settings = () => {
  return (
    <PageLayout activeLink="/settings">
      <div style={{ color: "white" }}>settings</div>
    </PageLayout>
  );
};

export default settings;
